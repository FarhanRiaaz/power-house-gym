import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFFFFC107); // Vivid yellow
  static const backgroundDark = Color(0xFF121212); // Deep black
  static const surface = Color(0xFF1E1E1E); // Slightly lighter black
  static const textPrimary = Color(0xFFFFFFFF); // White text on black
  static const textSecondary = Color(0xFFB0B0B0); // Muted gray
  static const danger = Color(0xFFD32F2F); // Red for errors
  static const success = Color(0xFF4CAF50); // Green for matched scans
  static const warning = Color(0xFFFFA000); // Orange for overdue
}
